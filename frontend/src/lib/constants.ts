/**
 * UI Constants for spatial layout and rendering
 */

export const PX_PER_CHAR = 20; // 20px width per character (12px char + 8px letter-spacing)
export const SECTION_WIDTH = 20; // 20px width for each 1/3 section of a word
export const PADDING_X = 0;    // No horizontal padding to keep grid alignment
export const GAP_X = 0;        // 0px gap between blocks for continuous MIDI indexing
export const INITIAL_PLAYHEAD_PADDING = 0;
